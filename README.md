# class-dna-database-greenberg

Title: proj4

Project goal: The goal of this project is to practice using hashing to store data.

Important files: dnadb.cpp, dnadb.h, dnadb.o, driver.cpp, driver.o, makefile, proj4


A C++ program that manages randomly generated DNA sequences via hashing.

Note that this was an academic assignment. That means that the function headers as well as what program should do were given by the professor. The Random class was also given.
